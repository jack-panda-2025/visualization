Prototype frontend

Open a browser to http://localhost:3001/ after starting the server (see server/ package.json).

This minimal UI fetches /api/metadata and lets you download level files for inspection.
